{
	"name": "humanmade/keyring-site-inventory",
	"description": "Read-only WordPress site-user roster for Keyring.",
	"type": "wordpress-muplugin",
	"license": "GPL-2.0-or-later",
	"authors": [
		{
			"name": "Human Made Limited",
			"email": "hello@humanmade.com"
		}
	],
	"require": {
		"php": ">=8.2",
		"composer/installers": "^1.0 || ^2.0"
	},
	"require-dev": {
		"humanmade/coding-standards": "^2.5"
	},
	"config": {
		"allow-plugins": {
			"composer/installers": true,
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
	"scripts": {
		"phpcs": "phpcs",
		"phpcbf": "phpcbf",
		"lint": "php -l keyring-site-inventory.php && php -l inc/service-user.php && php -l inc/site-inventory.php"
	}
}

